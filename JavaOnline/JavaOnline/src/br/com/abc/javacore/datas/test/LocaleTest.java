package br.com.abc.javacore.datas.test;

import java.text.DateFormat;
import java.util.Calendar;
import java.util.Locale;

public class LocaleTest {
	public static void main(String[] args) {
		/*
		 * ISO639 pt - português BR - Brasil eu - inglês us - Estados Unidos
		 */
		/*
		 * Locale locale = new Locale("it", "IT"); Locale locale2 = new
		 * Locale("it","CH"); Calendar c = new Calendar.getInstance(); c.set(2015,
		 * Calendar.DECEMBER, 21); DateFormat df =
		 * DateFormat.getDateInstance(DateFormat.FULL, locale); DateFormat df2 =
		 * DateFormat.getDateInstance(DateFormat.FULL, locale2);
		 * System.out.println(df.format(c.getTime()));
		 * 
		 * System.out.println(locale.getDisplayLanguage(locale));
		 * System.out.println(locale2.getDisplayLanguage(locale2));
		 * System.out.println(locale2.getDisplayCountry(locale2));
		 */
	}
}
